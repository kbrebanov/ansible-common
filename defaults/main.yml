---
# defaults file for common

fqdn: localhost.localdomain
hostname: "{{ fqdn.split('.')[0] }}"
domain: "{{ '.'.join(fqdn.split('.')[1:]) }}"
dns_servers:
  - 8.8.8.8
motd_ascii_logo: ""
motd_message: "Welcome to {{ fqdn }}"
