---
# defaults file for common

common_fqdn: localhost.localdomain
common_iptables_rules:
  - protocol: tcp
    source_addresses: 0.0.0.0/0
    port: 22
    comment: "OpenSSH"
common_dns_servers:
  - 8.8.8.8
common_motd_ascii_logo: ""
common_motd_message: "Welcome to {{ common_fqdn }}"
